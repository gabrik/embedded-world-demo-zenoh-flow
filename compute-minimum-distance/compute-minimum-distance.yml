id: compute-minimum-distance
vars:
 BASE_DIR: "/home/gabri/Workspace/embedded-world-demo-zenoh-flow"
uri: "file://{{BASE_DIR }}/compute-minimum-distance/compute-minimum-distance.py"

inputs:
  - id: scan
    type: LaserScan

outputs:
  - id: distance
    type: float32