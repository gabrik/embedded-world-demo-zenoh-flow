id: compute-proximity
vars:
 BASE_DIR: "/home/gabri/Workspace/embedded-world-demo-zenoh-flow"
uri: "file://{{BASE_DIR }}/compute-proximity/compute-proximity.py"

configuration:
  max_range: 0.8
  min_range: 0.2
  number_of_proximity_levels: 4

inputs:
  - id: distance
    type: float32

outputs:
  - id: light
    type: light_ctrl