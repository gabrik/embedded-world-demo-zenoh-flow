id: compute-minimum-distance
vars:
 BASE_DIR: "/home/ato"
uri: "file://{{BASE_DIR }}/embedded-world-zf/compute-minimum-distance/compute-miminum-distance.py"

inputs:
  - id: scan
    type: LaserScan

outputs:
  - id: distance
    type: float32