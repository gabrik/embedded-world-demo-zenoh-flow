id: compute-minimum-distance
vars:
 BASE_DIR: "/home/ato/Workspace/embedded-world-demo-zenoh-flow"
uri: "file://{{BASE_DIR }}/compute-minimum-distance/compute-miminum-distance.py"

inputs:
  - id: scan
    type: LaserScan

outputs:
  - id: distance
    type: float32